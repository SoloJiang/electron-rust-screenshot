{
  "name": "electron-rust-screenshot",
  "version": "0.1.0",
  "description": "",
  "main": "dist/lib.js",
  "types": "dist/index.d.ts",
  "directories": {
    "doc": "docs"
  },
  "napi": {
    "name": "electron-rust-screenshot",
    "triples": {
      "defaults": true,
      "additional": []
    }
  },
  "scripts": {
    "build": "napi build --platform --release --cargo-cwd crates/napi-bindings dist --js index.js --dts index.d.ts",
    "test": "node scripts/test-smoke.js",
    "validator:build": "cargo build --package validator",
    "validator": "cargo run --package validator -- validator-specs/*.toml --out validator-output"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@napi-rs/cli": "^2.18"
  }
}
